[build]
  command = "npm run build"
  publish = "dist"

# /escape-club was renamed to /give — keep old links working.
[[redirects]]
  from = "/escape-club"
  to = "/give"
  status = 301
  force = true
